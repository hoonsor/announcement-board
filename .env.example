# 本專案（靜態內容網站）本身不會在執行階段讀取任何環境變數，
# 以下變數僅供部署／自動化工具（GitHub、Vercel CLI）使用，
# 請勿提交含有實際金鑰的 .env 檔案（.env* 已列於 .gitignore）。

# GitHub 個人存取權杖（Personal Access Token），用於 git push／GitHub API
GITHUB_TOKEN=

# Vercel 存取權杖，用於 Vercel CLI／API 部署
VERCEL_TOKEN=
